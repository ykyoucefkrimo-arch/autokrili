*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Artefacts SQLite locaux : le projet tourne sur MySQL, ces fichiers sont des
# restes d'essais et contiennent des donnees de demonstration.
/autokree
/autokrili
/database/*.sqlite
# Scripts jetables de mise au point, executes via `artisan tinker`.
/_*.php

# --- Branche « demo » uniquement ---------------------------------------
# Le serveur de demonstration n'a pas Node : les assets compiles, les
# photos de demonstration et le dump de la base voyagent donc avec le
# code. Sur « main » ces trois chemins restent exclus.
!/public/build
!/storage/app/public/vehicles
